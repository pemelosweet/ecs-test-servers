# Parse Server 配置
PARSE_APP_ID=xmg
PARSE_MASTER_KEY=your-master-key-here
# 服务端内部寻址，可选（默认 http://localhost:1337/parse）
# PARSE_SERVER_URL=http://localhost:1337/parse
# 对外访问地址（文件/头像 URL 前缀、Dashboard），可选：config.js 按环境推导（开发回环 / 生产 admin 域名）
# PUBLIC_SERVER_URL=https://admin.xmg111.xyz/parse

# MongoDB 连接
DATABASE_URI=mongodb://localhost:27017/ecs

# Dashboard 配置（Dashboard 与主服务同端口，路径 /dashboard）
PARSE_DASHBOARD_USER=admin
PARSE_DASHBOARD_PASSWORD=admin123

# 阿里云 OSS 文件存储（配置 OSS_BUCKET_NAME 后启用，否则回退 MongoDB GridFS）
OSS_BUCKET_NAME=your-bucket-name
OSS_REGION=cn-beijing
OSS_ACCESS_KEY_ID=your-access-key-id
OSS_ACCESS_KEY_SECRET=your-access-key-secret
# 可选：自定义 OSS 端点（默认按 OSS_REGION 拼接 S3 兼容端点）
# OSS_ENDPOINT=oss-cn-beijing.aliyuncs.com

# 图床对外访问地址：生产建议绑定自定义域名后填写（如 https://img.xmg111.xyz），
# 否则回退到 OSS 原生域名。注意 OSS 默认域名对匿名请求可能强制下载，见 docs/image-hosting.md
# OSS_PUBLIC_URL=https://img.xmg111.xyz

# 图床/头像访问模式（默认 false = 走 Parse /parse/files 代理取图，可预览）：
# true = 直连 OSS/CDN 地址（需绑定自定义域名，否则默认域名可能强制下载）
# OSS_DIRECT_ACCESS=false

# 图床参数（可选）
# IMAGE_HOST_MAX_SIZE_MB=10   # 单图大小上限（MB）
# IMAGE_HOST_KEY_PREFIX=images # OSS 对象 key 前缀

# 阿里云号码认证服务（短信验证码，注册必填；未配置时 smsSend 报“短信服务未配置”）
ALIBABA_CLOUD_ACCESS_KEY_ID=your-access-key-id
ALIBABA_CLOUD_ACCESS_KEY_SECRET=your-access-key-secret
# 号码认证控制台“赠送签名/赠送模板”页面选择的签名与模板 CODE
SMS_SIGN_NAME=your-sign-name
SMS_TEMPLATE_CODE=100001
# 可选：方案名称（不填为“默认方案”，发送与校验必须一致）
# SMS_SCHEME_NAME=注册

# 阿里云图形认证（号码认证服务-图形认证方案管理控制台获取 appId/appKey）
GRAPHIC_CAPTCHA_APP_ID=your-app-id
GRAPHIC_CAPTCHA_APP_KEY=your-app-key

# Parse Dashboard 登录
PARSE_DASHBOARD_USER=xxx
PARSE_DASHBOARD_PASSWORD=xxx
# 服务端口
PORT=1337

# 初始管理员账号（启动时自动确保存在：无该用户则创建，已有同名用户则提升为管理员、保留原密码）
# 默认用户名：xmg001；创建时的默认密码 admin123456（生产环境务必用环境变量覆盖）
# ADMIN_INIT_USERNAME=xmg001
# ADMIN_INIT_PASSWORD=your-strong-password
